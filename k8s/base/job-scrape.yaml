apiVersion: batch/v1
kind: Job
metadata:
  name: scrape
spec:
  template:
    spec:
      containers:
        - name: scrape
          image: localhost:32000/scrape:0.0.0
          imagePullPolicy: Always
          command: ["bash","-c"]
          args: ["echo Hello World"]
          envFrom:
            - secretRef:
                name: web
      restartPolicy: Never
  backoffLimit: 0