apiVersion: batch/v1
kind: Job
metadata:
  name: migrate
spec:
  template:
    spec:
      containers:
        - name: web
          image: localhost:32000/kubeweb:0.0.0
          imagePullPolicy: Always
          command: ["bash","-c"]
          args: ["echo Running on: $RDS_HOSTNAME && python manage.py migrate"]
          envFrom:
            - secretRef:
                name: web
            - secretRef:
                name: shared
      restartPolicy: Never
  backoffLimit: 0